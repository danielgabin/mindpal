# Database
DATABASE_URL=postgresql://mindpal:mindpal_dev_password@localhost:5432/mindpal

# Elasticsearch
ELASTICSEARCH_URL=http://localhost:9200

# JWT Configuration
# IMPORTANT: Change these in production! Generate strong secrets with: openssl rand -hex 32
JWT_SECRET_KEY=your-super-secret-jwt-key-change-this-in-production
JWT_REFRESH_SECRET_KEY=your-super-secret-refresh-key-change-this-in-production
JWT_ALGORITHM=HS256
ACCESS_TOKEN_EXPIRE_MINUTES=15
REFRESH_TOKEN_EXPIRE_DAYS=30

# CORS
BACKEND_CORS_ORIGINS=["http://localhost:3000","http://localhost:3001"]

# API Configuration
API_V1_PREFIX=/api/v1
PROJECT_NAME=MindPal Clinical Notes

# Google Gemini API (for AI features - add your key here)
GOOGLE_GEMINI_API_KEY=your-google-gemini-api-key-here

# Email Configuration (stubbed for now)
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=your-email@example.com
SMTP_PASSWORD=your-email-password
EMAILS_FROM_EMAIL=noreply@mindpal.com
EMAILS_FROM_NAME=MindPal

# Environment
ENVIRONMENT=development
